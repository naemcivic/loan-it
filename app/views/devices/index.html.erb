<h2>
<%= Time.now.strftime("%H:%M     -    %A    -    %d/%m/%Y") %>
</h2>
<h3> Welcome </h3>
<h1>Honda</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @devices.each do |device| %>
      <tr>
        <td><%= device.name %></td>
        <% if device.active_loan.present? %>
        <td>Device Unavailable</td>
        <% else %>
        <td><%= link_to 'Loan-IT', new_device_loan_path(device) %></td>
        <% end %>
        <td><%= %></td>
        <td><%= link_to 'Show loan history', device_loans_path(device) %></td>




      </tr>
    <% end %>
  </tbody>
</table>






<br>

<%= link_to 'Admin', rails_admin.dashboard_path %>

<%= link_to 'Create New Group', new_group_path %>



