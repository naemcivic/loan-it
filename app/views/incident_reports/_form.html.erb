<%= form_for([@device, @incident_report]) do |f| %>
  <% if incident_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(incident_report.errors.count, "error") %> prohibited this incident_report from being saved:</h2>

      <ul>
      <% incident_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div align="center">
    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :description, :style => "text-align: center" %>
    </div>


    <%= f.hidden_field :user_id, value: current_user.id %>

    <div class="field">
      <%= f.check_box :usable, checked: true %>
      <%= f.label :usable, for: "incident_report_usable" %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
